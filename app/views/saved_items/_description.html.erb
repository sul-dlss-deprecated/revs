 <% if saved_item.id.to_s != edit_id or not can? :update, saved_item%>
    <% if can?(:update, saved_item) && saved_item.description.blank? %>
      <p class="muted">
    	  <em><%= t('revs.favorites.you_can_add_a_note_html', :href => link_to(t("revs.favorites.you_can_add_a_note_linktext"), edit_saved_item_path(saved_item.id,:page=>params[:page],:order=>params[:order]), :remote=>true)) %></em>  
      </p>
    <% else %>
      <p class="item-annotation">
        <%=saved_item.description%>                
      </p>
    <% end %>

  <% else %>
    <%= form_for(saved_item, :remote=>true, :method=>:put) do |f| %>
      <%= hidden_field_tag :page,params[:page] %>
      <%= hidden_field_tag :order,params[:order] %>
      <%= f.text_area :description, :rows => '2', :class => "input-xlarge", :value=>saved_item.description %>
      <p><%= f.submit t('revs.nav.update'), :class => "flag-button btn btn-small" %> | <%=link_to t('revs.nav.cancel'),cancel_saved_items_path(saved_item.id,:page=>params[:page],:order=>params[:order]),:remote=>true%></p>
    <% end %>
  <% end %>
  