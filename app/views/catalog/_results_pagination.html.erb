<% if @response.total_pages > 1 %>
  <div class="row record-padding">
    <div class="col-md-12">
      <nav class="pagination-container text-center">
	       <%= paginate @response, :page_entries_info => page_entries_info(@response),
          :theme => :blacklight %>
      </nav>
    </div>
  </div>
<% end %>
