<div id="content" class="col-md-12">

  <h2><%=t('revs.nav.collections')%>
    <span class="prev-next">
        <%= page_entries_info @collections, :entry_name => t('revs.collection.name') %>
    </span>
  </h2>

  <div class="row results-controls">
    <div class="col-md-4 view-toggle">
      <div class='gallery_toggle hidden showOnLoad tabbable'>
        <ul class="nav nav-pills">
          <li><%= link_to(t('revs.nav.grid'), all_collections_path(params.merge(:view => nil)), :class=>"#{'active' if @view == 'grid'}") %></li>
          <li><%= link_to(t('revs.nav.detailed'), all_collections_path(params.merge(:view => 'detailed')), :class=>"#{'active'if @view == 'detailed'}") %></li>
        </ul>
      </div>
    </div>
  </div>

  <% if @view == 'grid' %>
    <%= render 'grid' %>
  <% else %>
    <%= render 'detailed' %>
  <% end %>

</div>

<div class="col-xs-12 collections-pagination-container">
  <%= paginate @collections %>
</div>
